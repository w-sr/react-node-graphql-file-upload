type PaginationTypes = {
  page: number;
  total: number;
  onPrev: () => void;
  onNext: () => void;
};

const Pagination = ({ page, total, onPrev, onNext }: PaginationTypes) => {
  return <></>;
};

export default Pagination;
